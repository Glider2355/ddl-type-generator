version: '3.9'

services:
  mysql:
    image: mysql:8.0
    container_name: mysql-testdb
    hostname: ${DB_HOST}
    env_file:
      - ../.env 
    environment:
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    ports:
      - "${DB_PORT}:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
